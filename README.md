# Step 2 - Advanced Node.js

Run the examples:
* Install Node.js with NVM (https://github.com/creationix/nvm)
* Switch in the terminal into the chapter directory
* Start the chapter app by ```npm i && node app.js```
